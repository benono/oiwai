{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Next",
            "runtimeExecutable": "next",
            "cwd": "${workspaceFolder}",
            "args": ["dev", "--turbopack"],
            "env": { "NODE_OPTIONS": "--inspect" }
        }
    ]
} 